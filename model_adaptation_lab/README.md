# Local Model Adaptation Lab

This directory contains the setup and configuration for the local model adaptation lab. It includes directories for training datasets, evaluation datasets, evaluation tasks, model security checks, and rollback artifacts.